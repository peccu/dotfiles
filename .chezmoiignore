README.md
LICENSE
initialize.sh
*~
script
{{ if not .home -}}
# only for home machine
.lemrc
.screenrc
.tmux.conf
{{- else if not .codesandbox -}}
# only for codesandbox
{{- else if not .codespaces -}}
# only for codespace
{{- end }}
