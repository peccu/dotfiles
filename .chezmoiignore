README.md
LICENSE
initialize.sh
{{ if ne .chezmoi.hostname "home" -}}
# only for home machine
.lemrc
.screenrc
.tmux.conf
{{- end }}
{{ if ne .chezmoi.homeDir "/project/home/peccu" -}}
# only for codesandbox
{{- end }}
{{ if ne .chezmoi.username "codespace" -}}
# only for codespace
{{- end }}
