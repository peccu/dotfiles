README.md
LICENSE
initialize.sh
{{ if not .home -}}
# only for home machine
.lemrc
.screenrc
.tmux.conf
{{- else if not .codesandbox -}}
# only for codesandbox
{{- else if not .codespace -}}
# only for codespace
{{- end }}
