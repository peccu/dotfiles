# -*-mode:conf-toml-*- vim:ft=toml

# ~/.local/share/chezmoi/.chezmoi.toml.tmpl
# =============================================================================
# Used to customize configuration when doing a `chezmoi init`.
#
# This template file will trigger prompts to fill-in machine-specific
# templateable values. The resulting file is then created at
# `~/.config/chezmoi/chezmoi.toml`
# See https://www.chezmoi.io/docs/how-to/
#
# {{- /* This file supports Go's text/template language. */}}

# umask
umask = 0o022
# show progress
progress = true

{{- /* Environment detection */}}
{{- /* ref. https://www.chezmoi.io/user-guide/machines/containers-and-vms/ */}}
{{- $home:= eq .chezmoi.hostname "home" }}
{{- $tool:= eq (env "TOOLTYPE") "tool" }}
{{- $codespaces:= env "CODESPACES" | not | not }}
{{- $codesandbox:= env "CODESANDBOX_HOST" | not | not }}

{{- /* Checks if running interactively, which is not the case for GitHub Codespaces */}}
{{- $interactive := stdinIsATTY }}

{{- /* Default value for Git user conf */}}
{{- $name := "peccu" }}
{{- $email := "peccul@gmail.com" }}

{{- if $interactive }}
{{-   $name = promptStringOnce . "name" "Git username for the author/committer" $name }}
{{-   $email = promptStringOnce . "email" "Git email address for the author/committer" $email }}
{{-   writeToStdout "💡 Tip: you can re-enter your name and email with `chezmoi init --data=false`.\n" }}
{{- else }}
{{-   if hasKey . "name" }}
{{-     $name = .name }}
{{-   end }}
{{-   if hasKey . "email" }}
{{-     $email = .email }}
{{-   end }}
{{- end }}

# Data block (Needs this line to prevent concat data block and previous conf)
[data]
    email = {{ $email | quote }}
    name = {{ $name | quote }}
    home = {{ $home }}
    tool = {{ $tool }}
    codespaces = {{ $codespaces }}
    codesandbox = {{ $codesandbox }}
